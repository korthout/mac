#!/bin/bash

set -ex

source "$HOME/bin/lib/brew-context.sh"

function config() {
	git --git-dir=$HOME/.cfg/ --work-tree=$HOME $@
}

trap 'echo Terminated $0; exit' INT

config pull

brew bundle check --verbose || true

prompt "Continue? y/n"

brew bundle install --verbose
brew cleanup

commit_and_push "Update" "$BREWFILE"
